# .Rprofile - Project-level R configuration
# This file runs before any R code in your project

# Set CRAN mirror
options(repos = c(CRAN = "https://cloud.r-project.org/"))

# Suppress package startup messages (optional)
options(tidyverse.quiet = TRUE)

# Print confirmation (optional - remove after testing)
cat("✓ CRAN mirror set to:", getOption("repos")["CRAN"], "\n")